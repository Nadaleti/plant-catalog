import React from 'react';

const filterMobile = (props) => {
  return (
    <div>
      {/* TODO: Design sidebar filter item */}
      {/* TODO: open new area to filter on sidebar */}
    </div>
  )
}

export default filterMobile;
